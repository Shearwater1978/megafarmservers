{
    "server": false,
    "datacenter": "{{ consul_datacenter }}",
    "data_dir": "{{ consul_data_dir }}",
    "encrypt": "{{ token }}",
    "log_level": "INFO",
    "enable_syslog": false,
    "leave_on_terminate": true,
    "start_join": [
        "{{ consul_join_ip }}"
    ]
}
